# Install mise for managing multiple versions of languages. See https://mise.jdx.dev/
yay -S --noconfirm mise-bin

# Note: yay handles package updates and installations, so we don't need to manually add repositories or update package lists
